// - fromBucket: S3 bucket name where raw email stored.
// - toBucket: S3 bucket name to store email attachments.
var config = {
  fromBucket: "blah",
  toBucket: "blah",
  "debug": "true"
};

module.exports = config
